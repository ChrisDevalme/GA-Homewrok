function reword(word) {
    let space = " ";
    let newword = "";
    let str = word.splice(" ");
  
    for (i = 0; i < word.char(i); i++) {
      if (str === " ") {
        word.splice
        newword.push()
      }
    }
  
}
console.log("Hello world! This is a test.")
